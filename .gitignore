# Secrets
.env
# Firebase service-account credentials (never commit)
firebase-demo/service-account.json
firebase-demo/stackbreak-*.json
**/*-firebase-adminsdk-*.json

# Node
node_modules/
npm-debug.log*

# Playwright
/test-results/
/playwright-report/
/blob-report/
/.cache/
.last-run.json

# OS
.DS_Store

# Local probe scratch output (committed evidence lives under results/<date>/ and results/latest.json)
/tmp-probe/
.superpowers/

# Intermediate per-service browser findings (merged into browser.json / latest.json)
results/*/findings/
